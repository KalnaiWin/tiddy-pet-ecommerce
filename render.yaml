services:
  - type: web
    name: tiddy-pet-ecommerce-frontend
    env: static
    rootDir: frontend
    staticPublishPath: dist
    buildCommand: npm install && npm run build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
